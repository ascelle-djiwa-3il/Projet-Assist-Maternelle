package service;
import java.time.LocalDate;
import java.util.Date;
import java.util.List;

import model.*;

public class FactoryService {

	    // Méthode pour créer un contrat
	   /* public Contrat creerContrat(int id, String dateDebut, String dateFin, Parent parent, Enfant enfants,   StrategieStandard strategie) {
	        Contrat contrat = new Contrat();
	        contrat.setParent(parent);
	        contrat.setEnfants(enfants);
	        contrat.setDateDebut(dateDebut);
	        contrat.setDateFin(dateFin);
	        contrat.setStrategieIndemnites(strategie);
	        // Tu peux aussi initialiser l'état du contrat ici, par exemple "Actif"
	        //contrat.setEtat(new EtatActif());
	        return contrat;
	    }*/

	    // Méthode pour créer une session de garde
	    /*public JourGarde creerJourGarde(int IdContrat, String dateJour) {
	    	JourGarde jour = new JourGarde();
	        jour.setIdContrat(contratId);
	        jour.setDateJour(dateJour);
	        jour.setHeureArrivee(null);
	        jour.setHeureDepart(null);
	        return jour;
	    }*/


	
}
